# dependencies
node_modules/

# build output — static/js/app.js (legacy) TETAP di-track (parity guard),
# bundle + i18n adalah hasil build dari src/
dist/
static/js/bundle.js
static/js/i18n.js

# data user & aset berlisensi — tidak pernah masuk repo (privasi + lisensi;
# lihat README "Data user"). Dulu piecemeal, _param_todo.json lolos — kini utuh.
data/

# state runtime agent (ingatan tool remember — lokal saja)
.agent-memory/

# state CLI lokal (rencana sesi, artifact)
.zcode/

# shell Tauri — build artifacts only; sources are tracked
agent-shell/target/
agent-shell/gen/

# lain-lain
*.tmp
*.log
.DS_Store
